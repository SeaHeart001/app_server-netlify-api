MONGODB_URI=mongodb+srv://haohai:haohai@cluster0.xp0s2qx.mongodb.net/firstCloud?retryWrites=true&w=majority
JWT_SECRET=ewgfvwergvwsgw5454gsrgvsvsd
JWT_EXPIRES_IN=7d
CORS_ORIGIN=*


# Optional: comma-separated DNS servers for local networks that refuse MongoDB Atlas SRV lookups.
DNS_SERVERS=8.8.8.8,1.1.1.1

# Optional: create the first admin only when both values are set.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-me
DEFAULT_ADMIN_NAME=管理员
DEFAULT_ADMIN_SEX=1
